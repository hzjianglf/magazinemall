<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.dao.logisticsTemplateDao">
	
	<select id="selectlogisticsTemplat" resultType="hashmap" parameterType="int">
	   select * from logisticstemplateitem where  templateId = #{postageTempId} and isDel = 0
	</select>
</mapper>
	
